Deselect the one test that counts pytest's warnings; pytest 9.1 emits one more than it expects.

--- a/PKGBUILD
+++ b/PKGBUILD
@@ -21,7 +21,9 @@
 
 check() {
   cd pytest-asyncio
-  PYTHONPATH="$PWD" pytest
+  # test_asyncio_mark_respects_the_loop_policy counts warnings (4) and pytest
+  # 9.1 emits one more; the plugin is fine, the test is not
+  PYTHONPATH="$PWD" pytest --deselect tests/markers/test_class_scope.py::test_asyncio_mark_respects_the_loop_policy
 }
 
 package() {
