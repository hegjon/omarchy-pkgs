diff --git a/tests/test_interpreters.py b/tests/test_interpreters.py
index ccf7e04..64e376e 100644
--- a/tests/test_interpreters.py
+++ b/tests/test_interpreters.py
@@ -36,7 +36,7 @@ def _subinterp_code(code):
     """Wrap code with preamble so xxhash can be imported in a subinterpreter."""
     import pathlib
     root = pathlib.Path(__file__).resolve().parent.parent
-    return 'import sys; sys.path.insert(0, %r); ' % str(root) + code
+    return code
 
 
 # ── test base class ─────────────────────────────────────────────────────
@@ -291,7 +291,7 @@ class TestConcurrentInterpreters(unittest.TestCase):
     def _preamble():
         import pathlib
         root = pathlib.Path(__file__).resolve().parent.parent
-        return 'import sys; sys.path.insert(0, %r); ' % str(root)
+        return ''
 
     def test_exec_xxh64(self):
         import concurrent.interpreters as ci
