Run m4's own test suite (checks/) but not gnulib's self-tests (tests/).

--- a/PKGBUILD
+++ b/PKGBUILD
@@ -47,7 +47,7 @@
 
 check() {
 	cd "$pkgname"
-	make check
+	make check SUBDIRS='. examples lib src doc checks po'
 }
 
 package() {
